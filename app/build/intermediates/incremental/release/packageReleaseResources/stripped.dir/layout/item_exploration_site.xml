<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    style="@style/DinosaurCard"
    android:layout_marginVertical="@dimen/margin_small"
    android:clickable="true"
    android:focusable="true"
    android:foreground="?android:attr/selectableItemBackground">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="@dimen/card_padding_standard">

        <!-- Icono del sitio -->
        <ImageView
            android:id="@+id/ivSiteIcon"
            android:layout_width="@dimen/image_size_large"
            android:layout_height="@dimen/image_size_large"
            android:background="@drawable/creature_image_background"
            android:padding="@dimen/padding_small"
            android:scaleType="centerInside"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            tools:src="@drawable/ic_fossil_site"
            android:contentDescription="Site icon" />

        <!-- Contenido de texto -->
        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:layout_marginStart="@dimen/margin_medium"
            android:layout_marginEnd="@dimen/margin_small"
            app:layout_constraintStart_toEndOf="@id/ivSiteIcon"
            app:layout_constraintEnd_toStartOf="@id/ivExploreArrow"
            app:layout_constraintTop_toTopOf="@id/ivSiteIcon">

            <TextView
                android:id="@+id/tvSiteName"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                style="@style/DinosaurText.CreatureName"
                android:textSize="16sp"
                android:maxLines="2"
                android:ellipsize="end"
                tools:text="🏛️ Sitio de Excavación Alpha" />

            <TextView
                android:id="@+id/tvSiteDescription"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="@dimen/text_spacing_small"
                style="@style/DinosaurText.CreatureDescription"
                android:textSize="13sp"
                android:maxLines="3"
                android:ellipsize="end"
                tools:text="Importante yacimiento fósil con múltiples especímenes de este período" />

        </LinearLayout>

        <!-- Flecha de exploración -->
        <ImageView
            android:id="@+id/ivExploreArrow"
            android:layout_width="@dimen/icon_size_small"
            android:layout_height="@dimen/icon_size_small"
            android:src="@drawable/ic_arrow_forward"
            app:tint="@color/primary_green"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            android:contentDescription="Explore site" />

        <!-- Etiqueta del tipo de sitio -->
        <TextView
            android:id="@+id/tvSiteType"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/text_spacing_medium"
            android:background="@drawable/tag_background"
            style="@style/DinosaurText.Tag"
            android:paddingHorizontal="@dimen/padding_small"
            android:paddingVertical="@dimen/padding_tiny"
            app:layout_constraintStart_toStartOf="@id/ivSiteIcon"
            app:layout_constraintTop_toBottomOf="@id/ivSiteIcon"
            tools:text="Yacimiento" />

    </androidx.constraintlayout.widget.ConstraintLayout>

</androidx.cardview.widget.CardView>